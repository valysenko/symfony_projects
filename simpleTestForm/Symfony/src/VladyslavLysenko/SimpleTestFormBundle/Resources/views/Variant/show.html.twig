{% extends '::base_admin.html.twig' %}

{% block body -%}
    <h1>Variant</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    {#<li>#}
        {#<a href="{{ path('variant') }}">#}
            {#Back to the list#}
        {#</a>#}
    {#</li>#}
    <li>
        <a href="{{ path('variant_edit', {,'question_id':q_id 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
