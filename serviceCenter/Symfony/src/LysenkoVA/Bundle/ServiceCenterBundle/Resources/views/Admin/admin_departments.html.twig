{% extends '::base.html.twig' %}

{% block content %}
    <div class="container" style="margin-bottom:25px;width:750px;padding-left:105px;padding-right:5px;">
        <h3>Our departments</h3>

        {% for department in departments %}
            <div style="
            width:250px;
            background: #000;
            margin-right:50px;
            margin-bottom:10px;
            padding-top:30px;
            padding-bottom: 30px;
            float:left;
            text-align:center;
            background-color: #f5f5f5;
             border: 1px solid #e3e3e3;
            border-radius: 4px;
            -webkit-box-shadow: inset 0 1px 1px rgba(0, 0, 0, .05);
             box-shadow: inset 0 1px 1px rgba(0, 0, 0, .05);
            ">
            {{ department }}<br>
                <a href="/admin/comlaints/{{ department.id }}">View complaints</a><br>
                <a href="/admin/managers/best/{{ department.id }}">View best managers</a><br>
                <a href="/admin/employee/department/{{ department.id }}">Employee management</a><br>
                <a href="/admin/contracts/unmanaged/{{ department.id }}">Unmanaged contracts</a>
            </div>
        {% endfor %}
    </div>


{% endblock %}

